/** Быстрый фильтр каталога. */
export interface QuickFilter {
  /** Идентификатор фильтра. */
  id: string
  /** Путь к картинке для `<NuxtImg>`; фото на белом фоне — оно вписывается в серую подложку. */
  img?: string
  /** Подпись под картинкой, например `Классические`. */
  name: string
}
